{
  "name": "uts-fetch-players",
  "version": "1.0.0",
  "description": "Lambda for fetching player data from sleeper and store in DDB",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf build",
    "build": "echo 'npx tsc && todo: zip this shit up, prep for upload to s3",
    "pack": "echo 'webpack this puppy up'",
    "upload": "echo 'todo: upload to s3",
    "deploy": "echo 'put terraform in this package, let it do the heavy lifting"
  },
  "author": "Gabe Fabian",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.8.2",
    "ts-loader": "^9.4.1",
    "typescript": "^4.8.4",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  }
}
